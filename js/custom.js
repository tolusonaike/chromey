jQuery().ready(function() {
       jQuery('#coda-feat-slider').codaSlider({
           autoSlide: true,
           autoSlideInterval: 4000,
           dynamicTabs: false,
           dynamicArrows:false,
           autoSlideStopWhenClicked: true
       });
   });
